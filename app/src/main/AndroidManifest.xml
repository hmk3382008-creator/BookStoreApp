<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookStoreApp">

        <activity
            android:name=".User.MyOrdersActivity"
            android:exported="false" />
        <activity android:name=".Seller.MerchantOrdersActivity" />
        <activity android:name=".Seller.ManageProductsActivity" />
        <activity android:name=".Seller.VendorDashboardActivity" />
        <activity android:name=".User.OrderDetailActivity" />
        <activity android:name=".Seller.MerchantOrderDetailActivity" />
        <activity android:name=".Admin.AdminOrdersActivity" />
        <activity android:name=".Admin.AdminOrderDetailActivity" />
        <activity android:name=".User.CheckoutActivity" />

        <activity
            android:name=".Admin.AdminProductActivity"
            android:exported="false" />
        <activity
            android:name=".Member.AdminMemberActivity"
            android:exported="false" />

        <activity
            android:name=".Member.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".Member.AdminActivity"
            android:exported="false" />
        <activity
            android:name=".View.MainActivity"
            android:exported="false" />
        <activity android:name=".Member.AdminHomeActivity" />
        <activity
            android:name=".Member.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>